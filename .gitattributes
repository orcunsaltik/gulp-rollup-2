# Auto detect text files and perform LF normalization
* text=auto

# Force LF for shell scripts and executable files
*.sh text eol=lf
bin/* text eol=lf

# Force CRLF for Windows specific files
*.bat text eol=crlf
*.cmd text eol=crlf
